// Stub file for OwnerTransactionHistoryScreen
import 'package:flutter/material.dart';

class OwnerTransactionHistoryScreen extends StatelessWidget {
  const OwnerTransactionHistoryScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Transaction History')),
      body: const Center(child: Text('Transaction History')),
    );
  }
}
